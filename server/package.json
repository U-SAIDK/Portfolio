{
  "name": "portfolio-contact-server",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Express dev server for the contact form (mirrors netlify/functions/contact.js for local development without the Netlify CLI)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^9.0.5"
  }
}

